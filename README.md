# XMind Viewer & Editor for VS Code

一款在 Visual Studio Code 中直接查看和编辑 `.xmind` 文件的强大插件。

## 主要特性

- **可视化编辑**：支持直接在 VS Code 中修改节点文本、添加/删除节点。
- **撤销/重做 (Undo/Redo)**：支持完整的编辑历史管理，可通过工具栏或快捷键操作。
- **多页 (Multi-sheet) 管理**：
    - **完美支持**：包含多个画布的 XMind 文件，底部页签轻松切换。
    - **新增与删除**：支持动态增加或删除 Sheet 画布。
- **样式与主题保留**：在编辑过程中保留 XMind 原始样式元数据，确保与官方软件的高度兼容。
- **布局自动识别与切换**：
    - **自动识别**：打开文件时自动匹配 XMind 原始布局（如：思维导图、逻辑图、组织结构图等）。
    - **实时切换**：可在编辑时随时切换当前画布的布局模式。
- **空白文件自动初始化**：新建空白 `.xmind` 文件后打开，插件会自动初始化为标准的 XMind 结构（含中心主题）。
- **深度兼容性**：保存的文件完全符合 XMind 官方标准，可直接用 XMind 软件打开。
- **高级 UI 体验**：
    - 精致的 SVG 图标。
    - 智能页签管理（长标题自动缩略，防换行）。
    - 支持缩放、平移、节点收起/展开。

## 使用方法

1. **直接打开**：双击 `.xmind` 文件或将其拖入 VS Code。
2. **编辑**：
    - **Enter**: 插入同级节点。
    - **Tab**: 插入子节点。
    - **Delete**: 删除节点。
    - **双击**: 编辑文本。
    - **Ctrl/Cmd + Z**: 撤销。
    - **Ctrl/Cmd + Shift + Z**: 重做。
3. **保存**：使用 `Ctrl+S` (Windows) 或 `Cmd+S` (Mac) 即可将更改同步回原文件。

## 最近更新 (v0.1.7)

- **UI 深度优化**：引入 SVG 图标，优化端到端视觉体验，解决长标题换行问题。
- **历史管理**：新增撤销/重做功能及对应的快捷键支持。
- **Sheet 管理**：增加 Sheet 删除功能。
- **兼容性增强**：实现非破坏性样式保留算法，确保复杂样式在跨平台编辑时不丢失。

---

**Enjoy!**
